// Arrow function

/* function saludo() {
    return 'Hola'
}

const funcionFlecha = () => {}
 */
/* function listaDeTareas (){
    return (
        <ul>
            {listado.map(elemento => (<li>{elemento.nombre}</li>))}
        </ul>
    );
}*/

// Destructurin de Objetos y arrays

/* const cuadrado = {
    x: 10,
    y: 10,
}

function calcularArea (x, y){
    return x * y;
}

calcularArea(cuadrado)

function Avatar ({ username, url}){
    return '<img src={url} alt={username}/>'
} */

// Spray operator o parametros rest
/* const array = [1,2,3,4,5];
const otroArray = [7,8,9,10];


const nuevoArray = [...array,...otroArray];

console.log(nuevoArray) */


/* const obj1 = {

    a:'a',
    b:'b',
    c:'c'
}

const obj2 = {
    d:'d',
    e:'e',
    f:'f'
}

const nuevoObjeto = {...obj1,...obj2}
console.log(nuevoObjeto);
 */

/* function Componente ({id,...props}){
    return <NuevoComponente key={id} {...props}/>
} */

// ESModules

/* export default function suma (a,b){
    ...
}

export default function resta (a,b){
    ...
}

import suma from './suma';
import resta from '.resta';

function calculadora (){
    suma();
    resta();
}
 */
// Operador ternario
/* const coche ={
    marca:'Tesla',
    model:'CyberTruck',
}

let modelo = coche.model ? coche.model : 'x';

console.log(modelo) */

// Aprender metodos arrays
// Aprender async y await y los promise


